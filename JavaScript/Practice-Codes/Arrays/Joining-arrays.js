const numbers = [1,2,3,4]

// joining the array elements (string type)
const joined = numbers.join(',')
console.log(joined)

// spliting the message into the array
const message = 'This is my first message'
const parts = message.split(' ')
console.log(parts)
